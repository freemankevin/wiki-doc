---
title: 镜像构建
description: Docker - 镜像构建
published: true
date: 2026-01-30T03:49:42.845Z
tags: dockerfile, docker, vue, java, docker build
editor: markdown
dateCreated: 2026-01-30T01:31:31.869Z
---

# 镜像构建

> Docker 相关的 镜像构建 问题收集
{.is-info}


---

## Java 应用

### Dockerfile 示例

```dockerfile
FROM eclipse-temurin:8-jdk
WORKDIR /home
ENV JAVA_OPTS="-Xms512m -Xmx1024m -Duser.timezone=Asia/Shanghai"
COPY target/*.jar app.jar
EXPOSE ${APP_PORT}
ENTRYPOINT ["sh", "-c", "java ${JAVA_OPTS} -jar app.jar"]
```

---

## Vue 应用

### Dockerfile 示例

```dockerfile
FROM nginx:latest
WORKDIR /home
COPY html/ /usr/share/nginx/html/
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

[返回 Docker](/docker)
